/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['pouknleezgeebybauodo.supabase.co','localhost', 'images.unsplash.com', 'cdn.pixabay.com', 'res.cloudinary.com'], 
    }
};

export default nextConfig;